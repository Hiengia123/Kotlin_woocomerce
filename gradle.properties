# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# ====================================
# PERFORMANCE OPTIMIZATIONS
# ====================================

# Increase memory allocation for faster builds
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8 -XX:+UseParallelGC

# Enable parallel build execution
org.gradle.parallel=true

# Enable Gradle caching for faster builds
org.gradle.caching=true

# Enable configuration on demand
org.gradle.configureondemand=true

# Enable Kotlin incremental compilation
kotlin.incremental=true

# Enable Kotlin compilation daemon
kotlin.daemon.jvmargs=-Xmx2048m

# ====================================
# ANDROID OPTIMIZATIONS
# ====================================

# AndroidX package structure
android.useAndroidX=true

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Enables namespacing of each library's R class
android.nonTransitiveRClass=true

# Enable R8 full mode for better optimization
android.enableR8.fullMode=true

# Enable Jetifier (if needed for old dependencies)
android.enableJetifier=false
